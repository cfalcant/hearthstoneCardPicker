<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <form action="/cards" method="post">
      <input type="integer" name="mana" placeholder="mana">
      <input type="integer" name="attack" placeholder="attack">
      <input type="integer" name="health" placeholder="health">
      <input type="text" name="description" placeholder="name/description">
      <input type="submit">
    </form>
      <h3>Your Current List:</h3>
      <!-- All entries in "cards" showing on "newCard" -->
          <% for (let i = 0;i<deck.length;i++) { %>
          <hr>
          <div>
            <p>Mana: <%=deck[i].mana %></p>
            <p>Attack: <%=deck[i].attack %></p>
            <p>Health: <%=deck[i].health %></p>
            <p>Name/description: <%=deck[i].description %></p>
            <a href="/cards/remove/<%=cards[i].id %>">Remove from List</a>
          </div>
          <% } %>
      <hr>
      <h1>All Cards:</h1>
<!-- All entries in "cards" showing on "newCard" -->
    <% for (let i = 0;i<cards.length;i++) { %>
    <hr> <!--"horizonatal rule" - places divider between results from each loop-->
    <div>
      <p>Mana: <%=cards[i].mana %></p>
      <p>Attack: <%=cards[i].attack %></p>
      <p>Health: <%=cards[i].health %></p>
      <p>Name/description: <%=cards[i].description %></p>
      <!--HREF below was originally left blank but we inserted a route "/cards/add"-->
      <a href="/cards/add/<%=cards[i].id %>">Add to List</a>
    </div>
    <% } %>

    </div>
  </body>
</html>
